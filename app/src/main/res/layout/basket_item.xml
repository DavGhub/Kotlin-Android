<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:contentPadding="@dimen/dim_10dp"
    app:cardCornerRadius="6dp"
    app:cardElevation="5dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground"
    >
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        >

        <ImageView
            android:id="@+id/basket_meal_image"
            android:layout_width="@dimen/meal_image_size"
            android:layout_height="@dimen/meal_image_size"
            android:src="@drawable/ic_fastfood"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <TextView
            android:id="@+id/basket_meal_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="Name"
            android:textSize="@dimen/contact_name_size"
            android:textColor="@color/text_color"
            android:layout_marginTop="8dp"
            app:layout_constraintTop_toBottomOf="@id/basket_meal_image"
            app:layout_constraintStart_toStartOf="@id/basket_meal_image"
            app:layout_constraintEnd_toEndOf="@id/basket_meal_image"/>

        <ImageView
            android:id="@+id/basket_rate_star"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/rate_star"
            app:layout_constraintTop_toBottomOf="@id/basket_meal_name"
            app:layout_constraintStart_toStartOf="@id/basket_meal_image"
            app:layout_constraintEnd_toStartOf="@id/basket_meal_rating"
            />

        <TextView
            android:id="@+id/basket_meal_rating"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="4.9"
            android:textSize="20sp"
            android:textColor="@color/text_color"
            android:layout_marginStart="8dp"
            app:layout_constraintTop_toTopOf="@id/basket_rate_star"
            app:layout_constraintBottom_toBottomOf="@id/basket_rate_star"
            app:layout_constraintStart_toEndOf="@id/basket_rate_star"
            app:layout_constraintEnd_toEndOf="@id/basket_meal_image"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>